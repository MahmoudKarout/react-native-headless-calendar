pre-commit:
  parallel: true
  commands:
    lint:
      run: yarn lint
    typecheck:
      run: yarn typecheck
    test:
      run: yarn test --maxWorkers=2 --coverage
    build-library:
      run: yarn prepare

commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
